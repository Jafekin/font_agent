
# TODO

> 说明：本清单用于跟踪当前版本（naive RAG + OCR 版式实验）与下一阶段（Agentic GraphRAG 毕设迭代）的功能完成情况。

## 已完成（当前仓库已具备）

- [x] Web 上传与分析入口（Django）：上传图片触发分析，支持返回结构化 Markdown/JSON 报告
- [x] 分析历史留存：自动记录分析结果，便于复盘与对比
- [x] Naive RAG baseline：嵌入（Chinese-CLIP）→ NumPy 检索 → 结构化 Prompt → LLM 生成，默认带引用输出
- [x] 索引构建与数据脚本：批量生成 `embeddings.npy` / `metadata.json` 等索引文件
- [x] OCR/版式识别实验模块：基于 PaddleOCR 的布局分析，输出列数、白口/黑口、边框等版式指标
- [x] API 基础能力：提供分析与历史查询接口（用于前后端/脚本调用）

## 待完成（面向 Agentic GraphRAG 毕设迭代）

### GraphRAG 数据与图谱

- [ ] 图谱构建器：由 `metadata.json` + 版式特征 + 引用/相似关系生成图谱节点/边（文献-卷-页-版本-馆藏-版式等）
- [ ] 图存储/查询层选型与落地：轻量本地图（SQLite/NetworkX/自定义）或图数据库（Neo4j 等），并封装统一查询接口
- [ ] GraphRAG 检索器：支持关系路径检索、属性过滤、可解释证据路径输出

### Hybrid 检索与一致性

- [ ] Hybrid 检索内核：向量检索与图检索融合（混排/重排、权重调节、过滤条件）
- [ ] 引用一致性校验：对生成引用进行存在性与 ID 映射校验，失败触发补检索/降级
- [ ] 结果缓存与 citation trace：缓存重复查询，输出可追溯的证据链路与可视化/导出

### Agentic 编排（Skills/Tools）

- [ ] Agentic Orchestrator：规划/回溯/反思/回退逻辑，支持多轮工具调用与自校验
- [ ] Skills 封装：OCR/版式、图检索、向量检索、重排、引用核验、报告汇总等模块化可调度
- [ ] 可观测性：记录每轮工具调用、检索参数、证据路径、置信度指标，支持调参与回溯

### 输出与标准化

- [ ] JSON-LD 标注：根据输出模板自动生成 JSON-LD，打通图谱与外部档案/知识库
- [ ] 输出模板收敛：统一 Markdown/JSON 字段与引用格式（便于评测与前端展示）

### 工程化与扩展

- [ ] 索引增量更新：为索引构建脚本增加 watch/增量模式，避免全量重建
- [ ] Celery 异步流水线：将批量识别、索引构建、图谱计算异步化
- [ ] 多语言界面与 API 翻译：中文/英文界面与输出结果翻译支持

