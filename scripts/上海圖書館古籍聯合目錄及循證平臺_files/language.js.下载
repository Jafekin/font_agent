
//中英文切换
$("a.lang").click(function(){
	var lang_code = $(this).attr("lang-code");
	//获取URL地址
	var url = window.location.href;
	//改为同步
	$.ajaxSettings.async = false;
	$.get(url, {language: lang_code}, function(data) {
		//刷新当前页面
		window.location.reload();
	});
});